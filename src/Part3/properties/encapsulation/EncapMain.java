package Part3.properties.encapsulation;

public class EncapMain {
    // so encapsulation is basically that the wrapping of the data and the member functions into the class
    // so the encapsulation works on the internal self stuff.
}
